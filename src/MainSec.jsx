import React from 'react'


// props is used to pass data from one component to another in app.jsx
const MainSec = (props) => {
  return (
    <div>MainSec {props.variable}</div>
  )
}

export default MainSec